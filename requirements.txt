# ==============================================================
# requirements.txt — Project Friday v5.0.0
# Platform: Windows Desktop (ASUS TUF) + USB Webcam (eMeet C960)
# ==============================================================

# HTTP & API
requests>=2.31.0

# Claude (Anthropic) AI + Vision — otak utama
anthropic>=0.40.0

# Suara — Edge-TTS Premium (suara Microsoft Neural, gratis, online)
edge-tts>=6.1.0

# Suara — Fallback offline native Windows (SAPI5)
pyttsx3>=2.90

# Suara — Fallback Google TTS
gTTS>=2.4.0

# Speech Recognition
# (SpeechRecognition sudah membundel flac-win32.exe untuk Windows,
#  jadi tidak perlu install flac terpisah seperti di Termux)
SpeechRecognition>=3.10.0
PyAudio>=0.2.13

# Computer Vision — webcam USB, deteksi & pengenalan wajah (LBPH)
# CATATAN: pakai HANYA opencv-contrib-python (bukan opencv-python juga) —
# keduanya sama-sama menyediakan modul 'cv2' dan akan saling menimpa jika
# diinstall berbarengan. opencv-contrib-python sudah mencakup semua fitur
# opencv-python DITAMBAH cv2.face (LBPH) untuk pengenalan wajah personal.
opencv-contrib-python>=4.9.0
numpy>=1.26.0

# Browsing Internet
ddgs>=0.6.0

# Cari & putar video YouTube (skills/video.py) -- cuma untuk cari/ambil
# URL video, TIDAK dipakai untuk download/simpan video
yt-dlp>=2024.1.0

# Terminal Warna
colorama>=0.4.6

# Environment variables
python-dotenv>=1.0.0

# ==============================================================
# CATATAN INSTALASI WINDOWS:
#
#   pip install -r requirements.txt
#
#   Jika PyAudio gagal build dari source, pakai wheel precompiled:
#     pip install pipwin && pipwin install pyaudio
#
#   PENTING — pakai Python 3.12 (BUKAN versi paling baru, mis. 3.14+):
#   wheel prebuilt PyAudio (dan pipwin) biasanya baru tersedia beberapa
#   bulan setelah rilis Python baru. Kalau install PyAudio gagal dengan
#   pesan soal "no matching distribution"/build error, itu tandanya
#   versi Python-mu terlalu baru — install Python 3.12 dari python.org
#   atau `winget install Python.Python.3.12`, lalu buat ulang venv
#   pakai `py -3.12 -m venv venv` (setup_windows.ps1 sudah otomatis
#   mengutamakan 3.12 kalau tersedia).
#
#   Audio player (wajib, tidak lewat pip):
#     mpv    → https://mpv.io/installation/  (atau: winget install mpv)
#     ffmpeg → https://ffmpeg.org/download.html (fallback ffplay)
#
#   Ollama (fallback offline, opsional):
#     https://ollama.com/download/windows
#     ollama pull qwen2.5:7b
# ==============================================================
