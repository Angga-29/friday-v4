# ==============================================================
# requirements.txt — Project Friday v4.0.0
# Platform: Termux (Android) + IP Webcam + Xiaomi Pad 7
# ==============================================================

# Computer Vision (contrib WAJIB untuk pengenalan wajah!)
opencv-contrib-python>=4.8.0
numpy>=1.24.0

# HTTP & API
requests>=2.31.0

# Google Gemini AI + Vision
google-generativeai>=0.7.0

# Suara — Edge-TTS Premium (suara Microsoft Neural, gratis)
edge-tts>=6.1.0

# Suara — Fallback
gTTS>=2.4.0

# Speech Recognition
SpeechRecognition>=3.10.0
PyAudio>=0.2.13

# Browsing Internet — ddgs (nama baru) atau duckduckgo-search (lama)
ddgs>=0.6.0

# Terminal Warna
colorama>=0.4.6

# Environment variables (opsional — untuk load API key dari .env)
python-dotenv>=1.0.0
